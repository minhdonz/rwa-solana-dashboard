# These keys are used ONLY by `npm run refresh` (scripts/refresh-snapshot.ts).
# The running app never reads them — it serves the static data/snapshot.json.
# Copy to .env.local and fill in before running the refresh script.

HELIUS_API_KEY=
BIRDEYE_API_KEY=
